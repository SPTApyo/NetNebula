aiohttp>=3.9
beautifulsoup4>=4.12
# BeautifulSoup HTML backend: about 20% faster than html.parser. Optional, the
# code falls back without it.
lxml>=5.0
tldextract>=5.1
firebase-admin>=6.5
# count() on collections (aggregation queries) landed in 2.11.
google-cloud-firestore>=2.11
# Reads scrapper/.env into the environment. Optional: every setting can be
# exported by hand, and the crawler starts without this package.
python-dotenv>=1.0
